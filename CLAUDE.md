# 마음공부 — 개발 가이드

## 패키지 관리

- **항상 `pnpm`** (npm, yarn 금지)

```bash
pnpm dev      # 개발 서버
pnpm check    # 타입체크 (tsc --noEmit)
pnpm build    # 정적 생성
pnpm format   # Prettier
```

작업 순서: 코드 → `pnpm check` → `pnpm format` → `pnpm build`

## 이 앱의 성격

일기입니다. **담담함**이 제품의 핵심이라, 요란한 것을 붙이면 망가집니다.

- 애니메이션은 종이의 물성을 흉내 내는 데만 씁니다. 주의를 끌려고 쓰지 않습니다.
- 카피는 짧고 건조하게. 격려하거나 다그치지 않습니다.
- 통계·연속기록·배지 같은 습관 유도 장치는 넣지 않습니다.

## 핵심단어는 주제가 아니다

권의 단어는 그 글이 무엇에 대한 것인지가 아니라, **생각이 시작된 자리**입니다.
감기 권은 병에 대한 책이 아니라 감기가 열어젖힌 생각이고, AI 권은 AI에 대한
책이 아니라 AI와 일하다 시작된 생각입니다. 단어는 방이 아니라 문입니다.

카피를 고칠 때 이 방향을 뒤집지 마세요. "단어를 정해두고 그에 맞춰 쓴다"로
읽히면 앱의 전제가 반대가 됩니다.

## 데이터 흐름

**쓰기와 읽기가 분리되어 있습니다. 이 경계를 흐리지 마세요.**

- 초고 → `localStorage` (`src/lib/drafts.ts`). 절대 서버로 보내지 않습니다.
- 공개된 글 → `content/**.md` → 빌드 시 정적 생성 (`src/lib/content.ts`, 서버 전용)

`src/lib/content.ts`는 `node:fs`를 씁니다. 클라이언트 컴포넌트에서 import하지 마세요.
목록 화면에는 본문 블록을 떼고(`stripBlocks`) `EntryMeta`만 넘깁니다.

## 조판

**베이스라인 그리드가 이 앱의 뼈대입니다.** 본문의 모든 세로 치수 — 행간,
문단 사이, 글 머리 높이, 판면 높이 — 는 `LEADING`의 배수여야 합니다.
하나라도 어긋나면 펼침면 좌우 페이지의 줄 높이가 맞지 않고, 그 순간
책이 아니라 웹페이지로 보입니다. 값은 `src/lib/layout.ts` 한 곳에만 둡니다.

- `parse.ts` 본문 → 블록 · `paginate.ts` 블록 → 장 · `Measurer.tsx` 실측
- **`Measurer`와 판면은 반드시 같은 컴포넌트(`BlockRow`)를 렌더해야 합니다.**
  둘이 어긋나면 장 나눔이 조용히 틀어집니다.
- 세로 flex에서 블록 높이는 형제와 독립적입니다. 이 전제가 깨지면 측정도
  깨지므로, 판면에 `position: absolute`나 음수 마진을 넣지 마세요.
- 문단은 지은이가 끊어둔 줄(`\n`)에서 조각으로 나뉘어 여러 장에 걸쳐
  실립니다. 이어지는 조각에는 `cont` 표시가 붙고 위 여백을 받지 않습니다.
- 한글은 어절을 끊지 않으므로(`keep-all`) 양쪽정렬을 쓰면 자간이 벌어져
  판면의 색이 망가집니다. 왼쪽 정렬을 유지하세요.

## 판면

고전적인 책의 여백 비율은 **안쪽 < 위 < 바깥쪽 < 아래**다. 안쪽을 넓게
두면 펼침면이 두 장의 종이로 갈라져 보인다. 쪽번호와 기둥제목은 판면
바깥 여백에 절대배치한다. 본문 흐름에 끼워 넣으면 판면 높이가 줄어
그리드가 깨진다.

기둥제목은 왼쪽 면에 책 이름, 오른쪽 면에 날짜. 장이 열리는 면에는
얹지 않는다.

## 스타일

- Tailwind v4. 색은 전부 `globals.css`의 CSS 변수를 거칩니다 (`@theme inline`).
- **색상 리터럴을 컴포넌트에 직접 쓰지 마세요.** 낮/밤 테마가 깨집니다.
- 예외는 표지 하나입니다. 천을 씌운 양장본은 낮이든 밤이든 색이 변하지
  않으므로, 두 테마에서 같은 `hsl()` 값을 씁니다 (`BookCard.tsx`의 `boards`).
  채도를 올리면 곧바로 UI 카드처럼 보이니 20% 아래로 눌러둡니다.
- 그림자는 `rgb(var(--shadow-warm) / α)`. 밤에는 `--shadow-warm`이 검정이 됩니다.
- 한국어 본문은 `word-break: keep-all` (body 전역). 어절 중간에서 끊기면 읽기 나빠집니다.

## 펼친 횟수

유일하게 바깥 저장소를 쓰는 곳입니다 (Upstash Redis REST, src/lib/views.ts).

- **환경변수가 없으면 조용히 null을 돌려줍니다.** 저장소를 안 붙였거나
  잠시 닿지 않는다고 해서 글 읽는 일이 막히면 안 됩니다. 이 성질을 깨지 마세요.
- 슬러그는 반드시 getBooks()로 검증합니다. 검증 없이 키를 만들면
  누구나 임의의 키를 저장소에 쓸 수 있습니다.
- 읽는 화면(서재, 책)은 정적 생성을 유지합니다. 숫자는 클라이언트에서
  가져와 채웁니다. 서버에서 미리 넣으려 하면 페이지가 동적으로 바뀝니다.
- 통계를 더 붙이지 마세요. 연속기록, 순위, 배지 같은 것은 이 앱의 성격과
  정면으로 어긋납니다. 조회수 하나도 사실 경계에 있는 기능입니다.

## 코딩 컨벤션

- `type` 선호, `interface` 자제
- `enum` 금지 → 문자열 리터럴 유니온
- `any` 금지
- `console.log` 남기지 않기
- 빈 `catch`에는 왜 무시해도 되는지 한 줄 남기기

## 주석

무엇을 하는지가 아니라 **왜 이렇게 했는지**를 씁니다.
`paginate`, `Measurer`, `Spread`의 회전 유도처럼 다시 읽을 때 헷갈릴 곳에만 답니다.
